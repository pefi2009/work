/*  import React, { useState } from 'react'
 
 const Test1 = () => {

  const elements=[
    {id:0,color:'red',car:'Ford'},
    {id:1,color:'blue',car:'BMW'},
    {id:2,color:'green',car:'Ferrari'},
    {id:3,color:'BlacK',car:'Rlozroiz'}
  ]
  const tv = {id:4,color:'white',car:'Lexus'}
    const [item,setItem]= useState(elements)
  
      const buy=()=>{
        const shir=[...item]
        shir.push(tv)
        setItem(shir) 
 
      }
  return (
     <div>
      {item.map((r)=>{
         return(<li>{r.car}</li>)
      })}
      <button onClick={buy}>buy</button>
      
     </div>
   )
 }
 
 export default Test1 */